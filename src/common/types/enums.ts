export enum Role{
    CUSTOMER = 'CUSTOMER',
    ADMIN = 'ADMIN',
    STYLIST = 'STYLIST'
}

export enum Gender{
    MALE = 'MALE',
    FEMALE = 'FEMALE',
    OTHER = 'OTHER'
}

export enum Token {
    VERIFICATION = 'VERIFICATION',
    EMAIL_VERIFICATION = 'EMAIL_VERIFICATION',
    PASSWORD_RESET = 'PASSWORD_RESET',
    REFRESH = 'REFRESH'
}