echo "✅ Husky pre-push hook is running!"

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] || [ "$branch" = "develop" ] || [ "$branch" = "test" ]; then
  echo "🚫 Direct push to $branch is not allowed. Please create a feature branch."
  exit 1
fi

npm run test:prepush
